#include <iostream>
using namespace std;

int x[8][8]; //0:空  1:有皇后
int put[8]; //若put[i]=j, 表示第i行j列放着一个皇后
int check(int i, int j); //检查在(i,j)处放上一个新皇后，是否满足条件
int putQueen(int ); //表示放置第i行的皇后


int check(int i, int j){
	/* 放上该皇后之前一定是满足条件的
	 * return 1: 局面满足条件
	 * return 0: else */
	for (int )
}

int main()
{
	for (int i)
	return 1;
}
